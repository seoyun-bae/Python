245p_exercise

5.
함수 내부에서 global x를 선언하면 함수 안에서 x를 수정할 때 새로운 지역 변수를 만드는 대신 함수 밖의 전역 변수 x를 직접 수정하게 되기 때문

6.
x의 값이 양수인 경우는 나타나지 않음

7.
sub() -> 2 3 sub(a=10) -> 10 3 sub(5,6) -> 5 6 sub(b=10) -> 2 10

8.
30 -10

9.
local 변수는 sub()함수 내부에서 정의된 지역 변수이다. 함수 외부에서는 접근할 수 없기 때문에 파이썬은 local 이라는 변수를 찾지 못해 오류를 낸다